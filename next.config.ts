import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Required for Vercel static export compatibility
  // Remove if using Node.js server on Vercel
};

export default nextConfig;
